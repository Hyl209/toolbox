# mp4-mp3 实现计划

目标：做一个可被拖拽调用的 mp4→mp3 Python 小工具，支持自定义输出路径、记忆默认输出目录、随时修改默认路径，并保留可给外部 UI import 的函数接口。

阶段：
1. 建立计划与进度文件
2. 实现核心转换模块
3. 实现默认输出路径配置记忆
4. 实现命令行拖拽入口
5. 验证与说明文档

约束：
- 保持实现简洁
- 仅在当前项目目录下写文件
- 不修改无关内容
- 优先使用 ffmpeg
